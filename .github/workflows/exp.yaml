name: Trigger crossplatform BOM

on:
  push:
  workflow_dispatch:

jobs:
  propagate:
    runs-on: ubuntu-latest

    steps:
      #      - uses: actions/checkout@v3
      - run: echo ${{ github.ref }}
